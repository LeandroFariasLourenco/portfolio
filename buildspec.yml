version: 0.2

phases:
  install: {
    'runtime-versions': {
      nodejs: '14'
    },
    commands: [
       "npm install -g yarn",
       "yarn install --production",
       "yarn build"
      ]
  }
  build: {
    commands: [
      "echo test"
    ]
  }