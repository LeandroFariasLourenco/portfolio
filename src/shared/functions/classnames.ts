const classnames = (obj: object): string => {
  let cssClasses: string = '';

  // Object.entries(([key, value]: [string, boolean]) => {
  //   if (value) {
  //     cssClasses += ` ${key}`;
  //   }
  // });

  return cssClasses;
};

export default classnames;
